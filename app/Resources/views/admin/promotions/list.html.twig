{% extends 'base.html.twig' %}

{% block main_title %}Promotions{% endblock %}

{% block main %}
    <div class="well">
        <div class="text-left">

            <a href="{{ path('admin_set_promotion_category') }}" class="btn btn-success btn-xs">Set promotion to category</a>
            <a href="{{ path('admin_set_promotion_product') }}" class="btn btn-success btn-xs">Set promotion to certain products</a>
            <a href="{{ path('admin_set_promotion_all_products') }}" class="btn btn-success btn-xs">Set promotion to all products</a>
            <a href="{{ path('admin_delete_promotion_products') }}" class="btn btn-danger btn-xs">Remove promotion from products</a>

        </div>
        <h3>All shop promotions</h3>
        <a href="{{ path('admin_add_promotion') }}" class="btn btn-success btn-xs">Add new promotion</a>
        <hr>
        <table class="table table-responsive">
            <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
                <th>Discount</th>
                <th>Start date</th>
                <th>End date</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            </thead>
            <tbody>
            {% for promotion in promotions %}
                <tr>
                    <td>{{ promotion.id }}</td>
                    <td>{{ promotion.name }}</td>
                    <td>{{ promotion.description }}</td>
                    <td>{{ promotion.discount }}</td>
                    <td>{{ promotion.startDate|date }}</td>
                    <td>{{ promotion.endDate|date }}</td>
                    <td>
                        <a href="{{ path('admin_edit_promotion', {'id': promotion.id}) }}" class="btn btn-warning btn-xs">Edit</a>
                    </td>
                    <td>
                        <form action="{{ path('admin_delete_promotion', {'id': promotion.id}) }}" method="post">
                            <button class="btn btn-danger btn-xs" type="submit">Delete</button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="navigation text-center" >
        {{ knp_pagination_render(promotions) }}
    </div>
{% endblock %}